export default function NotFound() {
	return (
		<main className="not-found">
			<h1>Job not found</h1>
			<p>
				Unfortunately, we could not find the requested page or job data.
			</p>
		</main>
	);
}
